# Invest_Management

## How to run

Install the project requirements from the repository root:

```bash
pip install -r requirements.txt
```

Then start the dashboard:

```bash
python portfolio_dashboard/main.py
```
